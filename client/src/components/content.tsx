import React from 'react';
import { Routes, Route, Link } from "react-router-dom"
import { InstanceBox } from './instance_box';

export class RouterView extends React.Component {
  render() {
    return (
      <></>
      // <Routes>
      //   <Route path="/" element={<InstanceBox />}></Route>
      //   <Route path="/about" element={<InstanceBox />}></Route>
      // </Routes>
    );
  }
}